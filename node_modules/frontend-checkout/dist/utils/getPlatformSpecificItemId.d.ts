import { ItemId, ProductType, SupportedPlatform } from '../types';
declare const getPlatformSpecificItemId: (platform: SupportedPlatform, itemId: ItemId, productType?: ProductType) => string | number;
export { getPlatformSpecificItemId };
