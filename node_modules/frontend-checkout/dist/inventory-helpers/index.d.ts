import { SupportedPlatform, StoreConfig } from './types';
import { Inventory } from './Inventory';
declare const helpers: Record<SupportedPlatform, (config: StoreConfig) => Inventory>;
export { helpers };
