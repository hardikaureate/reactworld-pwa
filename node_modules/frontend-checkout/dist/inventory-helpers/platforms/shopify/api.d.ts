import { ShopifyInventoryResponse } from './types';
import { StoreConfig } from '../../types';
declare class Client {
    private platformDomain?;
    private storeDomain?;
    private defaultHeaders;
    private apiUrl;
    constructor(config: StoreConfig);
    fetchInventoryByProductIds(ids: string[]): Promise<{
        data: ShopifyInventoryResponse;
    }>;
}
export { Client };
