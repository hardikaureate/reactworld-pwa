import { PlatformInventoryHelper, ItemId } from '../../types';
import { BigCommerceInventory } from './types';
declare class BigCommerceInventoryHelper implements PlatformInventoryHelper {
    private getItemIdInPlatformSpecificFormat;
    private getStorefrontIdsMap;
    private modifyInventoryResponse;
    fetchProductsInventory(itemIds: ItemId[]): Promise<BigCommerceInventory>;
    isProductAvailableForSale(itemId: ItemId): Promise<boolean>;
    getProductQuantity(itemId: ItemId): Promise<number | undefined>;
    getProductMinOrder(itemId: ItemId): Promise<number | undefined>;
    getProductMaxOrder(itemId: ItemId): Promise<number | undefined>;
    getProductPrice(itemId: ItemId): Promise<number | undefined>;
}
export { BigCommerceInventoryHelper };
