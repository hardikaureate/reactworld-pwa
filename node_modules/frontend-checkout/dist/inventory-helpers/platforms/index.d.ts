import { Inventory } from '../Inventory';
import { StoreConfig } from '../types';
declare const createShopifyInventoryHelper: (config: StoreConfig) => Inventory;
declare const createBigCommerceInventoryHelper: (config: StoreConfig) => Inventory;
export { createShopifyInventoryHelper, createBigCommerceInventoryHelper };
