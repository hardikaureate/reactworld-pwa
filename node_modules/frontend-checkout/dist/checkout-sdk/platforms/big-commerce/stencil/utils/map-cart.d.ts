import { BigCommerceStencilCart } from '../types';
import { Cart } from '../../../../../types';
/**
 * Maps a BigCommerce returned from the REST API
 * into a frontend-checkout Cart
 *
 * @param {BigCommerceStencilCart} cart
 * @returns {Cart} a checkout cart instance
 */
declare const mapCart: (cart: BigCommerceStencilCart) => Cart;
export { mapCart };
