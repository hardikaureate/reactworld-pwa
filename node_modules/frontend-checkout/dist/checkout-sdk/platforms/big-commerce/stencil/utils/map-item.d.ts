import { BigCommerceStencilCartItem } from '../types';
import { Item } from '../../../../../types';
/**
 * Maps a BigCommerce Line Item returned from the REST API
 * into a frontend-checkout Item to be added to the Cart instance
 * @see https://developer.bigcommerce.com/api-reference/storefront/carts/cart/getacart
 * @param {BigCommerceStencilLineItem} item
 * @returns {Item} Item instance with BigCommerce specific properties
 */
declare const mapItem: (item: BigCommerceStencilCartItem) => Item;
export { mapItem };
