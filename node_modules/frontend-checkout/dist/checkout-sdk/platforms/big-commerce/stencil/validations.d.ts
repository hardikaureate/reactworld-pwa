import { Item, ItemId } from '../../../../types';
declare const validateId: (id: ItemId) => void;
declare const validateLineItemIds: (ids: ItemId[]) => void;
declare const validateItem: (item: Item) => Item;
declare const validateItems: (items: Item[]) => void;
declare const validateLineItems: (items: Item[]) => void;
export { validateId, validateLineItemIds, validateItem, validateItems, validateLineItems };
