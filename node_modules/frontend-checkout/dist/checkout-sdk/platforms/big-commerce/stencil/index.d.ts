import { BigCommerceStoreConfig, Cart, Platform, Item, ItemId } from '../../../../types';
declare class BigCommerceStencilPlatform implements Platform {
    private storeName;
    private cartId?;
    private addToCartInitialRequest?;
    constructor(config: BigCommerceStoreConfig);
    private getRestApiToken;
    private getCheckout;
    getCart(): Promise<Cart>;
    addItemsToCart(items: Item[]): Promise<Cart>;
    updateItemsInCart(items: Item[]): Promise<Cart>;
    removeItemsFromCart(itemIds: ItemId[]): Promise<Cart>;
    getProductTrackingData(item: Item): {
        id: number;
        quantity: number;
        brand: string;
        variantId: any;
        name: any;
        price: any;
    };
    getItemIdInPlatformSpecificFormat(itemId: ItemId): number;
}
export default BigCommerceStencilPlatform;
