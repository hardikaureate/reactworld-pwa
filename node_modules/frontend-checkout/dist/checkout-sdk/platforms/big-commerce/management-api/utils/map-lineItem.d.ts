import { BigCommerceAPILineItemPayload } from '../types';
import { Item } from '../../../../../types';
/**
 * Maps a frontend-checkout Item into a BigCommerce Line Item
 * to be used by the REST API.
 *
 * @param {Item} item
 * @returns {BigCommerceAPILineItem}
 */
declare const mapLineItem: (item: Item) => BigCommerceAPILineItemPayload;
export { mapLineItem };
