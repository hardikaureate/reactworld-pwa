import { Item } from '../../../..';
export declare function mapIdToVariantId(items: Item[]): ({
    quantity: number;
    variantId: string;
} | {
    line: number;
    quantity: number;
    variantId: string;
})[];
