import { Cart, Item, PlatformApiType, SupportedPlatform, StoreConfig } from '../types';
import Checkout from './Checkout';
declare const checkouts: Record<SupportedPlatform, Partial<Record<PlatformApiType, (config: StoreConfig) => Checkout>>>;
export { checkouts, Cart, Item };
